# API

Laravel application: the kernel (`app/Core`), the modules (`app/Modules`), both admin panels, the
store API and background work. Project overview and conventions: [../../README.md](../../README.md)
and [../../CLAUDE.md](../../CLAUDE.md).

```sh
composer setup
composer check
php artisan module:list
```
