<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Intentionally empty. Operators are created with "php artisan admin:operator" and shops from
 * the operator panel, so nothing with a password is ever seeded by accident.
 */
class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        //
    }
}
